@*@{
        ViewBag.Title = "UploadFile";
    }

    <h2>UploadFile</h2>

    @using (Html.BeginForm("UploadFile", "Upload", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {

        <div>
            @Html.TextBox("file", "", new { type = "file" }) <br />

            <input type="submit" value="Upload" />

            @ViewBag.Message

        </div>


    }*@

@model IEnumerable<string>
@{
    var UpFiles = (List<tblUploadFile>)ViewBag.UpFiles;
}
@{
    ViewBag.Title = "Index";
}

<h2>Files</h2>

<ul>
    @foreach (var fName in Model)
    {
        var name = fName;
        var link = @Url.Content("~/UploadedFiles/") + name.Replace(" ", "%20");

        <li>
            <a href="@link">@name</a>
        </li>
    }
</ul>

<div>
    @using (Html.BeginForm("UploadFile", "Upload", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @*<div class="form-group">
            <label class="control-label col-md-3">Đơn vị</label>
            <div class="col-md-9">
                @Html.DropDownList("idDonVi", null, htmlAttributes: new { @class = "form-control" })
                @Html.ValidationMessageFor(model => UpFiles., "", new { @class = "text-danger" })
            </div>
        </div>*@

        <input type="File" name="file" id="file" value="Choose File" />
        <button type="submit">Upload</button>
    }
</div>